
--Read Operations:
SELECT * FROM covid_data;

SELECT country_region, confirmed, deaths, recovered 
FROM covid_data;

SELECT country_region, confirmed, deaths, recovered 
FROM covid_data WHERE country_region = 'uganda';











